
# Python build artifacts
__pycache__/
*.pyc

# Claude Code agent worktrees (isolated checkouts for background subagents —
# never repo content, and may be actively in use by a running agent)
.claude/worktrees/

# ClickUp ingest corpus — contains customer names, balances and phone numbers.
# Staged locally for the build; never committed.
.clickup-ingest/
